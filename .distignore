# Tutto cio' che elencato qui NON viene pubblicato su wordpress.org.
# Lo zip scaricato dagli utenti deve contenere solo i file del plugin.

/.git
/.github
/.githooks
/.wordpress-org
/tests
/bin
/vendor
/node_modules
/build

.gitattributes
.gitignore
.distignore
.git-blame-ignore-revs

# Artefatti generati dagli strumenti. Non bastava .gitignore: la action di
# deploy copia dal disco, non da git, quindi un file ignorato da git ma
# presente nella cartella finisce comunque su wordpress.org.
.phpunit.result.cache
.phpcs-cache
composer.json
composer.lock
phpcs.xml.dist
phpunit.xml.dist
README.md
